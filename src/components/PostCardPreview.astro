---
// PostCardPreview is now identical to PostListPreview.
// Kept for backward compatibility with existing page imports.
import type { CollectionEntry } from "astro:content";
import PostListPreview from "./PostListPreview.astro";

type Props = { post: CollectionEntry<"blogs"> };
const { post } = Astro.props;
---

<PostListPreview post={post} />
